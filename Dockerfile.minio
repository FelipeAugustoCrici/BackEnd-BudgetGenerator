FROM minio/minio:latest

VOLUME ["/data"]

EXPOSE 9000
EXPOSE 9001

# Railway injects PORT env var — minio uses it via the command
CMD ["sh", "-c", "minio server /data --address :${PORT:-9000} --console-address :9001"]
