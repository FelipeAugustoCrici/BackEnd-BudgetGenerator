# ── AUTH SERVICE ─────────────────────────────────────────────────────────────
# Adicione estas variáveis no serviço "auth" do Railway:

DATABASE_URL=${{Postgres.DATABASE_URL}}
JWT_SECRET=troque-por-uma-chave-segura-aleatoria

# ── CORE SERVICE ─────────────────────────────────────────────────────────────
# Adicione estas variáveis no serviço "core" do Railway:

DATABASE_URL=${{Postgres.DATABASE_URL}}
JWT_SECRET=troque-por-uma-chave-segura-aleatoria
MINIO_ENDPOINT=${{minio.RAILWAY_PRIVATE_DOMAIN}}:9000
MINIO_ACCESS_KEY=budgetgen
MINIO_SECRET_KEY=budgetgen123
MINIO_BUCKET=uploads
MINIO_PUBLIC_URL=https://${{minio.RAILWAY_PUBLIC_DOMAIN}}
